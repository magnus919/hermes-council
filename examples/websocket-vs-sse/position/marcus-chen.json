```json
{
  "position": "Start with Server-Sent Events as the default, measure actual traffic patterns, and upgrade specific channels to WebSocket only when the data proves bidirectional or sub-100ms latency is a measured requirement, not an assumed one.",
  "reasoning": [
    "Most real-time notification systems are overwhelmingly unidirectional — server pushes status updates, badge counts, or alerts to clients. SSE maps directly to this pattern with zero protocol overhead for the reverse channel.",
    "SSE has automatic reconnection built into the EventSource API, HTTP-level auth and routing, and works through existing infrastructure (load balancers, proxies, CDNs) without WebSocket upgrade negotiation or sticky-session configuration.",
    "The 'what if we need bidirectional later' argument is the most expensive justification in real-time architecture. It usually leads to WebSocket complexity for a channel that stays unidirectional forever. WebSocket adds: subprotocol negotiation, heartbeat management, reconnection logic, message framing, and a fundamentally different operational model for connection draining and scaling.",
    "A polyglot strategy works in practice. I've run systems where 80% of channels stayed on SSE and 20% (collaborative cursors, live-typing indicators, document sync) used WebSocket. The SSE channels had zero migration cost because they were never the bottleneck.",
    "Total system complexity is the hidden cost. WebSocket systems need: sticky sessions (or a shared session store), connection state management outside the request-response cycle, custom health-check endpoints for load balancers, and monitoring for half-open connections. SSE inherits HTTP's operational model — it's just a long response that keeps writing.",
    "Team expertise is the deciding variable, not protocol capability. A team that has run WebSocket in production for three years will build a reliable WebSocket system. A team that hasn't will fail regardless of protocol choice. SSE is lower-risk for teams without proven real-time operations experience."
  ],
  "concerns": [
    "SSE has a hard limit on concurrent connections per browser (typically 6 per domain via HTTP/1.1, alleviated by HTTP/2 multiplexing). If you need hundreds of open connections per page, WebSocket wins on connection budget alone.",
    "SSE doesn't natively support binary frames. If your notifications include binary payloads (protobuf-encoded messages, images, or audio fragments), you're adding base64 overhead or building a framing layer on top of SSE anyway — at which point WebSocket's native binary support becomes attractive.",
    "The EventSource API is limited: no custom headers (critical for auth tokens), no support for POST-based subscription, and no standard way to specify which events to receive. Polyfills and wrapper libraries close these gaps but add dependencies that the 'simpler' SSE path was supposed to avoid.",
    "Backpressure is harder with SSE. A slow consumer on a WebSocket can signal the sender to slow down via the bidirectional channel. With SSE, the server has to implement its own backpressure detection (buffered amount, TCP window monitoring) or accept that a slow client will buffer indefinitely and eventually disconnect.",
    "Browser tab throttling affects SSE differently. Browsers may defer or throttle EventSource connections in background tabs, while WebSocket connections (once established) generally survive tab throttling. For notification channels that must be near-realtime even when the tab is backgrounded, this matters.",
    "SSE connection count on the server side is deceptively expensive. Each SSE connection holds an open file descriptor and a response writer. With WebSocket, the same resources are consumed but the per-connection overhead of the upgrade handshake and subprotocol negotiation is front-loaded. SSE looks cheaper per-connection until you hit the thousands — at scale, the operational models converge."
  ],
  "confidence": 0.72,
  "evidence_needed": [
    "Measured traffic directionality ratio: what percentage of notification channels actually send data from client to server? This is the single most impactful data point.",
    "Measured latency requirements per channel type, not assumed. What does 'real-time' mean for each notification category — badge count (seconds acceptable) vs collaborative cursor position (100ms target)?",
    "Concurrent connection count per user session and per page. Are we looking at 3 connections or 300? This determines whether SSE's browser connection limit is theoretical or binding.",
    "Existing infrastructure maturity: does the team already run WebSocket in production with established patterns for connection draining, sticky sessions, and half-open detection?",
    "Payload characteristics: average and P99 notification size, binary vs text ratio, and whether payloads need compression or encoding transformation.",
    "Authentication and authorization model for real-time channels: can SSE's stateless cookie/token model serve the auth requirements, or does the system need per-message authorization that maps better to WebSocket's explicit connection-level identity?"
  ]
}
```